turn.turnLeft(turn.leftOptions(90))
forward.forward(forward.forwardOptions(Forward.Once, turn.totalDegrees()))
turn.turnLeft(turn.leftOptions(Left.FortyFive))
forward.forward(forward.forwardOptions(Forward.Once, turn.totalDegrees()))
